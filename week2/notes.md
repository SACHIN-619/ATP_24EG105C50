porperty is in the form of key:value pair


advnced op in array(es6)

filter(call)

w: callback function is a function which is passed as parametr inside the function
    who calls the callback function?
      ans: application



shift,unshift,push,pop affect original array

but filter,map(),forEach(),reduce(),find(),findInde,sort() make a copy of that array

instead of let why const??

reduce(2 parameter optional)
        ^|^
accumulator(after fist sum it recieve sum<-acumulatro(which initallly reciece first )+element(2nd))


