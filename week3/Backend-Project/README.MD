1.Generate package.json
2.Create  express server
3.install mongoose and connect to MongoDB ever
    RST API ---MongoDB native driver-->DB Server
    REST API ----MONGOOSE ODM(objDocMag) tool-->DB server   means js oj mapped tomongodb obj
------------------------------------------------------------------------------------------

cont.. on day 7 in day 6 work.

4.Build USER REST API
-Create User
- Read all users
-Read user by ID
-Update a User by ID
-Delete a User by ID
5.Create Schema and Model of the Reasource(User)  schema->Structure of the doc.
6.Create USER API and Define the route



-->Handeling unavailable resources.
-->valdators during update
-->Hashing password(bcrypt.js)
-->Unique fieds
-->Refined version of error hndling middleware

------------------------------------------------
type of attacking while storing in browser
XSA-crossside scripting attack

when we store http cookies in broswer it return empty string
doc.cookies http->once must be ticked
but rather storing in local or session storage .


CSRF - crossside resource forging 





