Imperative programming: we have call funs like querselctor for reading and adddEvenListioner again and again for each and ervy task for each tag so its difficulat as we have call again and again same thing.

Declrative Programming:-we dont have to anything like Imperative its easy.

React works not real DOM but Virtual DOM like vanila js modifieson direct DOM which is time consuming.

commands to follow for reactjs:-

     npm create vite@latest
     hooks-> Function (state)

     useState- makes comp to reredner n dispaly latest value
     import {useState} from 'react'

     inside function const [count-->state,setCount-->function to modifet state] = useState(0)
     call setCount(count+1)  // it change the state
     useEffect
     userContext
     useRef
     useMemo